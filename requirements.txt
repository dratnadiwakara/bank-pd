# Pinned to the versions installed in C:\envs\bank-pd-venv (Python 3.14.4 on Windows).
# Note: wrds is pinned to pandas<2.3 in its setup.cfg but is functionally compatible
# with pandas 3.x. Install with --no-deps to avoid a hard pandas downgrade.

duckdb>=1.5
pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.31
pyyaml>=6.0
python-dateutil>=2.8
matplotlib>=3.7
openpyxl>=3.1
pytest>=7
yfinance>=0.2

# WRDS client and its runtime deps (avoid pandas 2.2 pin via --no-deps)
psycopg2-binary>=2.9
sqlalchemy>=2.0
# wrds itself: install last with --no-deps:
#   pip install --no-deps wrds>=3.5
